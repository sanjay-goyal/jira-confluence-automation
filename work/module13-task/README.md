# Module 13 Task

This folder is reserved for the MCP server setup and integration work for Module 13.

## Goal
- Configure and validate a local MCP server entry in VS Code.
- Keep the setup lightweight and easy to test.
- Use the server configuration in `.vscode/mcp.json` as the entry point.

## Structure
- `.vscode/mcp.json` — VS Code MCP configuration
- `scripts/mcp-echo.ps1` — sample local echo server script
- `backlog.md` — work tracker for the module
