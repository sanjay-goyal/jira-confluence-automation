# Python Calculator

This is a simple Python calculator project.

## Files

- `calculator.py` contains the `add()`, `subtract()`, and `multiply()` functions.
- `main.py` imports and uses the calculator functions to print sample results.

## Run

Run the project with:

```bash
python main.py
```